export const APP_NAME = "movie-browser";

export const STORAGE_KEYS = {
  FAVORITES: `${APP_NAME}:favorites`,
  GENRES: `${APP_NAME}:genres`,
  SEARCH_HISTORY: `${APP_NAME}:search-history`,
};

export const API_BASE_URL = "/api";
